These files are ES6 versions of:

    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^5.2.0",

Only reason this repository exists is because React doesn't ship them... (what are they paid for?!)